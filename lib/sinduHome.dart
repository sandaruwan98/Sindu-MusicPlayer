import 'package:flutter/material.dart';

class SinduHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
          
        ),
    );
  }
}
